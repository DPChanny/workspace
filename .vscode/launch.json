{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug C++",
      "type": "lldb-dap",
      "request": "launch",
      "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
      "cwd": "${fileDirname}",
      "preLaunchTask": "Build C++",
      "stdio": ["stdin", null, null]
    },
    {
      "name": "Debug C",
      "type": "lldb-dap",
      "request": "launch",
      "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
      "cwd": "${fileDirname}",
      "preLaunchTask": "Build C",
      "stdio": ["stdin", null, null]
    },
    {
      "name": "Debug Python",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "cwd": "${fileDirname}"
    }
  ]
}
